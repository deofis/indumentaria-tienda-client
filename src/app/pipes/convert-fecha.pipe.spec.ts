import { ConvertFechaPipe } from './convert-fecha.pipe';

describe('ConvertFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
